```{r}
pacman::p_load(tidyverse, here, data.table)
```

# General data wrangling

```{r}

```

```{r}
setwd(here("Data", "Exports raw"))

exports_raw <- 
  here("Data", "Exports raw") %>% 
  dir(pattern = "*.rds") %>% 
  map(read_rds) %>% 
  reduce(rbind)
```

```{r}
setwd(here("Data", "Imports raw"))

imports_raw <- 
  here("Data", "Imports raw") %>% 
  dir(pattern = "*.rds") %>% 
  map(read_rds) %>% 
  reduce(rbind)
```

```{r}
setwd(here())
```
